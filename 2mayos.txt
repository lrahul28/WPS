Kernel module

-Module management system
the module management system allows modules to be loaded into memory and to communicate to the rest of the kernel
-Module loader and unloader 
they are user load utilities
to load a module into the memory
-driver registration system allows to tell that a new driver is availableto the rest of the kernel 
-conflict resolution mechanism
it allows diff device drivers to reserve hardware resources and to protect those resources from another driver
-process management
fork()
exec()
process identity
